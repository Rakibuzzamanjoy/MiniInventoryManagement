# Mini Inventory Management API
A simple RESTful Web API built with .NET 8 and Entity Framework Core for managing products and customer orders.

## Features
- Product Management (CRUD operations)
- Order Management with automatic stock reduction
- Repository Pattern with Dependency Injection
- Entity Framework Core (Code First)
- Swagger/OpenAPI documentation

## Prerequisites
- .NET 8.0 SDK
- SQL Server (LocalDB, Express, or full version)

## Setup Instructions

1. **Clone the repository**
   ```Open Git Bash
   git clone https://github.com/Rakibuzzamanjoy/MiniInventoryManagement.git
  
2. Update the connection string in `appsettings.json` 
